/* Furigana markup: {漢字|かな}. Parse once; render as <ruby> or bare kanji
   depending on what the learner knows; derive the kana spelling from the
   kana side of each run. See README → Data → Furigana markup. */
