/* Grammar patterns. Examples are filtered to known words at runtime; a
   pattern is offered once at least two qualify. */
const PATTERNS = [
{id:"wa-desu", pat:"〜は〜です", m:"X is Y", st:4,
 ex:[["わたしはがくせいです。", "I am a student."]]},
];
